#pragma once
#ifndef PREPROCESS
#include <bits/stdc++.h>
#endif
using namespace std;

#include <algo/utils/loops.h>

using ll = long long;
using pii = pair<int, int>;
using pll = pair<ll, ll>;
using vi = vector<int>;
using vll = vector<ll>;

#define all(x) begin(x), end(x)
#define sz(x) (int)(x).size()
#define fi first
#define se second
